# Dependencias de ejecución
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
SQLAlchemy>=2.0,<3.0
argon2-cffi>=23.1
pyotp>=2.9,<3.0
segno>=1.6,<2.0
cryptography>=41.0

# Controlador MySQL (usado por docker-compose.mysql.yml; inofensivo con SQLite)
pymysql>=1.1

# Controlador PostgreSQL para BD gestionada gratis (p. ej. Neon) en despliegues
# serverless/efímeros como Cloud Run, Koyeb o Render. Inofensivo con SQLite/MySQL;
# con una URL postgresql://… SQLAlchemy usa psycopg2 por defecto.
psycopg2-binary>=2.9
